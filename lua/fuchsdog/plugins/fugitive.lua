return {
	"tpope/vim-fugitive",
	lazy = false,
	config = function()
		-- Необязательно: дополнительные настройки, если нужны
		-- vim.cmd("command! -nargs=0 G :Git") -- Пример команды для быстрого доступа
	end,
}
